export namespace Route {
  export enum Path {
    HOME = "/",
    CATALOG = "/catalog",
    PRODUCT = "/product/:id",
    CONTACTS = "/contacts",
  }
  export enum Name {
    HOME = "home",
    CATALOG = "catalog",
    PRODUCT = "product",
    CONTACTS = "contacts",
  }
}
